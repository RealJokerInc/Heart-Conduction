"""cardiac_ml.training: Trainer, callbacks, default step functions.

Public exports are populated by Phase 3 (trainer, callbacks, mlflow_logger,
default_steps). This __init__ stays minimal to avoid forcing heavy imports.
"""
